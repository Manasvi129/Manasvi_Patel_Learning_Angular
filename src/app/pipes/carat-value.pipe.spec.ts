import { CaratValuePipe } from './carat-value.pipe';

describe('CaratValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CaratValuePipe();
    expect(pipe).toBeTruthy();
  });
});
